import React from 'react';
import styles from './Spinner.module.css'

const spinner = () => ( 
        // We just find spinner css codes from googling :)

    <div className={styles.Loader}>
        Loading...
    </div>
)

export default spinner;